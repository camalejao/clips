{
  "name": "clips",
  "version": "1.0.0",
  "main": "src/server.js",
  "license": "MIT",
  "scripts": {
    "dev": "node src/server.js",
    "heroku-postbuild": "cd client && yarn && yarn build",
    "postbuild": "cd client && yarn && yarn build",
  },
  "dependencies": {
    "axios": "^0.21.1",
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "fluent-ffmpeg": "^2.1.2",
    "ytdl-core": "^4.9.1"
  }
}
